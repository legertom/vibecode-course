# /check

Verify the current lesson's success criteria against real state (files, commands, live URL).
On pass: praise, update `progress.json`, announce any badge, and offer the next lesson.

Follow the `/check` rules defined in CLAUDE.md.
